/*Задача 2:
Можем да използваме няколко команди заедно:
$ history | sort -k 2 | sed 's/^ *[0-9]\ *//' | uniq -c | sort -n | less
Отпечатва от историята на командите колко пъти се стартира
всяка*/

history | sort -k 2 | sed 's/^ *[0-9]\ *//' | uniq -c | sort -n | less
