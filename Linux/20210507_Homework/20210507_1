/*Create a directory in your home directory called projects. In the projects
directory, create nine empty fi les that are named house1, house2, house3, and so on to house9. 
Assuming there are lots of other fi les in that directory, come up with a single argument to ls that would list just those nine files.*/

cd ~                  /* Влизаме в home директорията */
pwd                   /* Проверяваме, дали сме в правилната директория, т.е в home */
mkdir projects        /* Създаваме директорията projects в home директорията */
ls                    /* Проверяваме дали сме създали директорията */
cd projects           /* Влизаме в projects директорията */
pwd                   /* Проверяваме, дали сме в правилната директория, т.е в projects */
touch house{1..9}     /* Създаваме файловете house, които са с номера от 1 до 9 */
ls или ls house*      /* Проверяваме, дали създаването на файловете е било успешно */
