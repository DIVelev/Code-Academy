#include <stdio.h>

int main(void)
{
    int x = 10;
    int y = 10;
    int true;
    // Спрямо приоритета на операторите - първо шифтва x с 1, след това е побитовото и (&), а най-накрая е логическото и (&&)
    true = x&&y&x<<1;
    printf("Result : %d\n",true);

    // Спрямо приоритета на операторите - първо шифтва x с 1, след това е побитовото изключващо или (^,XOR), а най-накрая е логическото и (&&)
    true = x&&y^x<<1;
    printf("Result : %d\n",true);

    return 0;
}
